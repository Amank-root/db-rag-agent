# Deploy Detective — local environment (everything optional)
# NOTE: model-provider and Daytona keys belong in TrueForge Settings, never here.

# Override the world database location (default: deploy-mcp/data/incident-world.sqlite)
# D2_DB_PATH=/absolute/path/to/incident-world.sqlite