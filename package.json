{
  "name": "deploy-detective",
  "private": true,
  "version": "1.0.0",
  "description": "Deploy Detective (D2) — approval-gated incident response + analytics agent on TrueForge",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "build": "npm --prefix deploy-mcp run build",
    "seed": "npm --prefix deploy-mcp run seed",
    "reseed": "npm --prefix deploy-mcp run reseed",
    "test": "npm --prefix deploy-mcp test",
    "smoke": "bash scripts/smoke.sh",
    "create-agent": "tsx scripts/create-agent.ts"
  },
  "dependencies": {
    "@truefoundry/trueforge-sdk": "^0.1.0"
  },
  "devDependencies": {
    "tsx": "^4.20.0"
  }
}